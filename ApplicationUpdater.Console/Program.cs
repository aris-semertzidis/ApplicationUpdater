//using System.Text.Json;
//using ApplicationUpdater.Core;
//using ApplicationUpdater.Core.Builder;

//Console.WriteLine("Let's start building your project. Please paste the folder");
//string folder = Console.ReadLine()!;

//BuildManifest manifest = ApplicationBuilder.CreateManifest(folder);
//string commit = ApplicationBuilder.CreateCommitVersionFromGit(folder);
//manifest.commit = commit;
//File.WriteAllText(Path.Combine(folder, "manifest.json"), JsonSerializer.Serialize(manifest, new JsonSerializerOptions { WriteIndented = true }));


Console.ReadKey();


////Updater updater = new("https://www.google.com");


